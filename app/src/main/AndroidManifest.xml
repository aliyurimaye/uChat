<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.aliyu010.uchat">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        tools:replace="android:appComponentFactory"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".registration"></activity>
        <activity android:name=".ActivityLecCourseCreate" />
        <activity android:name=".MessageActivity" />
        <activity android:name=".ActivityLecCourseView" />
        <activity android:name=".ActivityCoursesView" />
        <activity android:name=".ActivityCoursesReg" />
        <activity android:name=".Menu"/>
        <activity android:name=".tutorMenu"/>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>